import React from 'react';

const fallback = () => (
  <div>
    <h1>Apologize but looks like your device is offline.</h1>
    <small>Please check your network setting.</small>
  </div>
);

export default fallback;